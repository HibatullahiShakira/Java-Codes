import java.util.Scanner;

   	public class Integer {
       
   	   public static void main(String[] args){
	 Scanner inputs = new Scanner(System.in);

	System.out.println("Enter an integer:");
        int number = input.nextInt();
        
	int square = number * number;
        int sum = square + number;
 
	if (sum > 100);
	System.out.println("The number and its square is greater than 100");

  	if (sum < 100)
	System.out.println("the number and its square is less than 100:");

	if (sum==100);
	System.out.print("The number and its square is not equal to 100:");
	if (sum != 100);
	System.out.println("The number and its square is not equal to 100");
 }
} 